const {
  addStudentMenu,
  dropStudentMenu,
  studentMenu,
} = require("./student-menu");
const { mainMenu } = require("./main-menu");
const appointmentMenu = require("./appointment-menu");

module.exports = {
  addStudentMenu,
  dropStudentMenu,
  mainMenu,
  studentMenu,
  appointmentMenu,
};
